class Category {
	constructor(id, title, color, firstContent, secondContent) {
		this.id = id;
		this.title = title;
		this.color = color;
		this.firstContent = firstContent;
		this.secondContent = secondContent;
	}
}

export default Category;
